# Evidence snapshot retention and degraded-database resilience: acceptance criteria

Plan: `docs/plans/evidence-snapshot-resilience/plan.md` · Branch: `feat/evidence-snapshot-resilience`

Pass conditions are **invariants**, not equality against a config literal. No test or Pass cell pins the retention window, timeout or interval value; tests read the named definition. Agent/QC/Reviewer cells stay empty until independent verification.

| ID | Plan ref | Role | Acceptance criterion | Pass (invariant) | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AC-FN-1 | D1 | backend-architect | Creating a snapshot prunes expired ones, bounded per call | A create call deletes expired snapshots oldest-first, at most the named batch size, and a backlog drains over the next calls; every snapshot inside the window, including the new one, remains, with all its entries | PASS (stream A): I wrote my own probe at scratchpad/probe.mts. It applies migrations 20260329, 20260913x2, 20260914 and 20260915, then 20260919 byte for byte, and seeds 7 applications. The window came from the named function (86400s), and I… | PASS (main thread, post-OCR): re-verified after the prune was bounded — batch-bound test added; unbounded prune fails it | |
| AC-FN-2 | D1 | backend-architect | Pruning cascades to entries | After a prune, no `application_evidence_snapshot_entries` row references a deleted snapshot | PASS (stream A): Probe: after the prune, the orphan-entry query (entries with no parent snapshot) returned 0, and the pruned IDs had 0 entries each (the just-outside snapshot had 7 before). This relies on the existing FK `snapshot_id ...… | PASS · trust | |
| AC-FN-3 | D1 | backend-architect | Paging inside the window is unaffected | A snapshot created inside the window pages end to end through `get_application_evidence_snapshot_page` with every ordinal returned exactly once, including after a later create call has run its prune | PASS (stream A): Probe: I read page 1 (limit 3) of the just-inside snapshot, ran a create that pruned two expired snapshots, then paged to the end. The 7 application_ids came back once each, in the same order as the entries table's ordinals.… | PASS · trust | |
| AC-FN-4 | D1 | backend-architect | A pruned snapshot refuses cleanly | Paging a pruned snapshot ID yields the existing `snapshot_not_found` refusal (P0001 at the RPC, the named refusal at the MCP tool), not an error or an empty page | PASS (stream A): Probe: get_application_evidence_snapshot_page on a pruned ID raised {code:'P0001', message:'Evidence snapshot not found'}. It returned no page at all, empty or otherwise. I also registered the real… | PASS · trust | |
| AC-FN-5 | D1 | backend-architect | Retention is defined once | Exactly one definition of the retention window exists in the migration; the create RPC and tests reference it by name | PASS (stream A): The only literal for the window is `select interval '24 hours'` inside public.application_evidence_snapshot_retention() (migration line 38). The creator calls it by name: `where created_at < now() -… | PASS: window and batch size each defined once, read by name in tests | |
| AC-FN-6 | D1 | backend-architect | The migration is forward-only and preserves grants | The redefined RPC keeps its existing execute grants (service role only; not anon or authenticated) and security settings; the repo's security-definer audit still passes | PASS (stream A): Probe compared the creator's grants before and after applying 20260919. Both times: proacl '{postgres=X/postgres,service_role=X/postgres}', prosecdef true, proconfig ['search_path=pg_catalog, public'],… | PASS: grants/security restated; PGlite runs the exported definer-grant audit (0 rows). Live `check:rpc-grants` runs on apply (O1) | |
| AC-FN-7 | D4 | backend-architect | Shared client calls are time-bounded | With an underlying fetch that never resolves, a query through the shared client rejects within the named timeout plus a small tolerance | PASS (stream B): Probe fetch-probe.mts (in the scratchpad esrb folder). A0 is the most direct check: it imports the real shared client from src/lib/supabase.ts, pointed at a local TCP server that accepts connections and never replies, using… | PASS: unchanged; the shared ceiling can be tightened by callers but not extended (documented) | |
| AC-FN-8 | D4 | backend-architect | Normal calls are untouched | With a fetch that resolves, responses pass through unchanged, and a caller-supplied abort signal is still honored (combined, not replaced) | PASS (stream B): Passthrough (C): with a fetch that resolves, POST init is passed through unchanged (method POST, headers {"a":"b"}, body 'q', plus an added AbortSignal). The response is returned unchanged (status 200, custom header… | PASS · trust | |
| AC-FN-9 | D5 | backend-architect | Cleanup never overlaps itself | With a prune that never resolves, advancing fake timers across several intervals issues exactly one prune call | PASS (stream B): Probe cleanup-probe.mts, test 'C fake timers', using node:test t.mock.timers with setInterval. Uses startOAuthTokenCleanup with a prune that never resolves. Ticking OAUTH_TOKEN_PRUNE_INTERVAL_MS*10 gave prunes=1 and… | PASS: unchanged by the post-boot first-prune addition | |
| AC-FN-10 | D5 | backend-architect | The guard releases on failure | After a prune rejects, the next tick issues a new prune; a rejection is logged and never throws out of the timer | PASS (stream B): Test 'D fake timers': a prune that always rejects, ticked 3 intervals, called prune 3 times. Each call was logged as '[oauth] cleanup: token prune threw','db down'. A second handle mixes an in-band `{error:{message:'inband'}}`… | PASS · trust | |
| AC-FN-11 | D5 | backend-architect | In-memory auth-code sweep is preserved | Expired authorization codes are still evicted on their own cadence, independent of the database prune's state | PASS (stream B): The sweep runs on its own setInterval at AUTH_CODE_SWEEP_INTERVAL_MS=60000, the same cadence as before. The eviction logic in oauth.ts (`now > data.expires_at` then delete) is moved over verbatim. Fake-timer test C: with the… | PASS with note: the route-level `authCodes` sweep body is code-review only (private map); module-level cadence independence is tested | |
| AC-FN-12 | D5 | backend-architect | The timer does not hold the process open | The cleanup timer is unref'd, as today | PASS (stream B): Real timers: h.timers.prune.hasRef()=false and h.timers.sweep.hasRef()=false. unref-probe.mts starts the cleanup with a prune that never resolves and does nothing else. The process exited on its own after 2 ms with exit code… | PASS · trust | |
| AC-TST-1 | D2 | backend-architect | Retention tests run in the SQL suite | The new PGlite test file is registered in `test:application-evidence-source` and executes | PASS (stream A): package.json test:application-evidence-source now includes tests/application-evidence-snapshot-retention.test.ts. Running `npm run test:application-evidence-source` in the worktree gave tests 42, pass 42, fail 0, and the… | PASS: re-run after OCR fixes, test:application-evidence-source 44/44 | |
| AC-TST-2 | D6 | test-engineer | Resilience tests run in the unit suite | The new test files are registered in `test:unit` and execute; none needs network or `.env.local` | PASS (stream C): Registered: `git diff origin/main -- package.json` shows the only change is `tests/supabase-fetch-timeout.test.ts tests/oauth-token-cleanup.test.ts` appended to the end of `test:unit`, with no entries removed. They execute: in… | PASS: re-run after OCR fixes, test:unit 768/768 | |
| AC-DOC-1 | D3 | backend-architect | The source contract states retention | `docs/application-evidence-snapshot.md` states the window by referencing its named definition, that readers must finish paging inside it, and that a pruned ID returns `snapshot_not_found` | PASS (stream A): docs/application-evidence-snapshot.md has a new '## Retention' section. It names the definition `public.application_evidence_snapshot_retention()` in `20260919000000_application_evidence_snapshot_retention.sql` and tells… | PASS: retention section also states the per-call batch bound | |
| AC-REG-1 | — | test-engineer | No regression | `npm run build` passes; `test:unit` and `test:application-evidence-source` pass with no fewer tests than `main` | PASS (stream C): `npm run build` (tsc) on the branch at 67bd12a finished with exit 0 and no errors. Unit tests were run with the same dummy env on both branches (SUPA_PROJECT_URL=http://127.0.0.1:1 plus dummy keys): - Branch, `npm run… | PASS: after OCR fixes — tsc clean; unit 768/768; evidence-source 44/44 | |
| AC-PUB-1 | — | backend-architect | Public-repo hygiene | No file, commit message, or PR text on this branch names a private repo, org, client, host, or operational URL (CONTRIBUTING's Public-Repo Rule); checked on the live PR body, not a draft | PASS (stream A): the branch diff and commit messages were grepped for the Public-Repo Rule's forbidden-name list (kept out of this public doc by design) and for http(s) URLs; no hits | Files + commit messages clean (grep). Live PR body still to be checked at Phase 5 | |
