# Résumé wiki alignment: acceptance criteria

Plan: `docs/plans/resume-wiki-format/plan.md` · Branch: `feat/resume-wiki-format-impl` · Issue: #298

Pass conditions are **invariants**, not equality against a literal. Tests read budget values from `RESUME_BUDGET` and the threshold from `PASS_THRESHOLD` rather than repeating the numbers. Agent is filled by the verification sub-agent, QC by the main thread, Reviewer by the owner.

| ID | Plan ref | Role | Acceptance criterion | Pass (invariant) | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AC-FN-1 | D1 | backend-architect | Bullet grammar is enforced | No emitted project highlight or non-pinned employment bullet ends with a period (pinned bullets are verbatim by AC-FN-14); a standalone `&` between words becomes "and"; an `&` inside a token (e.g. "R&D") is unchanged | PASS (re-verify 2): highlights and non-pinned bullets cleaned; highlight-cleaning assertion added after pass 1 found no test | PASS · trust | |
| AC-FN-2 | D1 | backend-architect | Summary is bounded | The emitted summary has at most `RESUME_BUDGET.summarySentences` sentences; a period inside a token (e.g. "Node.js") is not treated as a sentence end | PASS (re-verify 1): summary cap tested through normalizeResumeFormat against RESUME_BUDGET | PASS · trust | |
| AC-FN-3 | D1 | backend-architect | Roles are ordered and budgeted | Emitted roles are in descending `start_date` order; the first has at most `mostRecentRoleBullets` bullets and every other role at most `otherRoleBullets` | PASS: ordering and both bullet budgets asserted via RESUME_BUDGET | PASS · trust | |
| AC-FN-4 | D1 | backend-architect | Projects and skills are budgeted | At most `projects` projects, each with at most `projectHighlights` highlights; categorized skills capped at `skillRows` rows; a flat string skills list passes through unchanged | PASS: projects, highlights and skill rows capped via RESUME_BUDGET; flat skills pass through | PASS · trust | |
| AC-FN-5 | D1 | backend-architect | Self-employment doesn't restate Projects | A self-employment bullet that names a featured project is dropped; a self-employment entry never ends up with zero bullets | PASS: restated bullet dropped, one always kept, short-name floor tested | PASS · added a test after /ocr-review that the dedupe is scoped to self-employment only | |
| AC-FN-6 | D1 | backend-architect | Post-processing never adds a metric | For every bullet, the digits in the output are a subset of the digits in the input: post-processing only removes or rewords | PASS: output digits a subset of input digits | PASS · trust | |
| AC-FN-7 | D2, D3 | backend-architect | Weak verbs are vetoed and replaced | Each newly banned phrase triggers the Rule 4 veto when present, and after `stripBannedPhrases` no banned phrase remains and no bullet is emptied by the replacement | PASS (re-verify 1): Rule 4 veto and exact replacements tested for each newly banned phrase | PASS · /ocr-review showed "functions as" and "responsible for" rewrote ordinary prose; both dropped from the banned list (the STAR/XYZ rule covers them as openers), leaving four newly banned phrases, each with exact replacement tests | |
| AC-FN-8 | D10 | test-engineer | STAR/XYZ is judged by meaning, off the request path | The judge counts a bullet only when it states a result that happened (numeric or not: scale, a met commitment, adoption, a capability delivered, a replacement, an end state) and rejects intentions, vague claims and pure duty; nothing in `src/` calls it. *Amended twice with owner approval on 2026-09-25: first a regex rule scored in the rubric (it matched wording, not meaning), then this.* | | | |
| AC-FN-9 | D2 | backend-architect | Pass bar is unchanged | `PASS_THRESHOLD` divided by the number of scored rules equals 4.0 / 5, the bar on `main` | | | |
| AC-FN-10 | D4 | backend-architect | Scoring sees the shipped output | In `/resume`, each candidate goes through `stripBannedPhrases` then `normalizeResumeFormat` before `scoreResume`, and the response body is the post-processed winner | PASS: strip → normalize → score in generate-resume.ts; route sends the post-processed winner | PASS · trust | |
| AC-FN-11 | D4 | backend-architect | Prompt states the new rules | The system prompt instructs: summary of at most 2 sentences; STAR/XYZ bullets with bullet grammar; never invent or estimate a metric; categorized skills with a `{ category, items }` response example; role bullet budget; 1–2 projects; no restating featured projects in self-employment | PASS: all required prompt rules present; budget numbers interpolated from RESUME_BUDGET | PASS · trust | |
| AC-FN-12 | D7 | backend-architect | Route and eval share one generation core | The `/resume` handler delegates generation, post-processing, scoring and winner selection to `generateResume`; the SSE response body keeps the same fields as before the refactor | PASS: route delegates to generateResume; _rubric fields match the base line for line | PASS · trust | |
| AC-FN-13 | D2 | backend-architect | Sync's banned-phrase gate widens as intended | `scripts/sync.ts` still reads `BANNED_PHRASES` from the rubric, so a proposed highlight containing any newly banned phrase is rejected | PASS: sync.ts imports BANNED_PHRASES and gates proposed highlights on it | PASS · trust | |
| AC-FN-14 | D9 | backend-architect | Pinned roles come from the profile, never the model | Each pinned role is emitted exactly once with the profile's company, title, dates and bullets verbatim and in order, exempt from caps and the self-employment dedupe; any model copy of it is dropped (same company and start date, same company when it's the profile's only role there, or same title and dates); a different role at the same company is kept; the model can never mark a role pinned. *Amended 2026-09-25: originally "restored from the profile over whatever the model returned". Verification probes showed reconciling model output with pinned roles duplicated or merged roles (two roles at one company, a repeated pinned company, a model-set pinned flag), so pinned roles are now shown to the model as context only and inserted from the profile.* | PASS against amended Pass: three probe rounds found duplication/merge defects in model reconciliation; redesign (profile is the only source) covers every probe case in tests | PASS · re-ran live eval after the pinned redesign: all cases pass incl. pinned-verbatim check | |
| AC-FN-15 | D2, D10 | backend-architect | Pinned roles are never scored or judged | Rule 4 skips pinned roles, and the eval's STAR/XYZ report covers only generator-written bullets | | | |
| AC-EVAL-1 | D8 | test-engineer | Eval cases span role types and stay synthetic | The cases cover at least 3 distinct role types, and no JD is copied from a real employer posting | PASS: four synthetic JDs across four role types | PASS · trust | |
| AC-EVAL-2 | D8 | test-engineer | Eval checks the shipped invariants | Each case passes or fails only on deterministic checks: format and budget invariants, categorized skills, absence of banned phrases, numbers grounded in the candidate's written text or the thoughts given to the model, pinned roles verbatim. The STAR/XYZ judge result is printed but never fails a case | | | |
| AC-EVAL-3 | D8 | test-engineer | Eval is on demand only | `npm run eval:resume` exists; the runner is not in `test:unit` and not in any workflow under `.github/workflows/` | PASS: eval:resume exists; not in test:unit or .github/workflows | PASS · trust | |
| AC-JDG-1 | D10 | test-engineer | Judge agreement is measurable | `npm run eval:star-judge` judges every labeled calibration bullet, reports agreement and names each disagreement; no calibration bullet appears in the judge prompt | | | |
| AC-JDG-2 | D10 | test-engineer | Stored bullets can be reviewed | `npm run check:bullets` reads the profile only, judges each role's stored bullets, and lists every miss with its reason | | | |
| AC-TST-1 | D5 | test-engineer | New tests run in the unit suite | The new and updated test files are registered in `test:unit` and execute without network access or `.env.local` | PASS: resume-format.test.ts registered; résumé test files run without .env.local | PASS · trust | |
| AC-DOC-1 | D6 | backend-architect | Pipeline doc matches the rubric | `docs/resume-pipeline-v2.md` lists the rules `scoreResume` actually returns (ids and names) and the current pass threshold | PASS (re-verify 2): rule ids, exact names, order and threshold match scoreResume | PASS · trust | |
| AC-SMK-1 | D4, D8 | backend-architect | Live output reflects the changes *(smoke-only: needs live generation)* | `npm run eval:resume` runs against the live profile on this branch; every case passes or its failure is explained in the review report | PASS: live eval passed all cases in verification pass 1 | PASS · main thread re-ran eval:resume after the AC-FN-8 amendment and after the pinned redesign; all cases passed both times | |
| AC-REG-1 | — | test-engineer | No regression | `npm run build` passes and `test:unit` passes | PASS: build and test:unit pass | PASS · trust | |
| AC-PUB-1 | — | backend-architect | Public-repo hygiene | No file, commit message or PR text on this branch names a private repo, org or person, or contains personal data | PASS: no private repo, org, person or personal data in diff or commits | PASS · after /ocr-review, test fixtures use fictional product names and roles | |
